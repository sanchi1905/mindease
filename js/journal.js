/*
  js/journal.js — deprecated
  Recording and journal saving moved to `src/components/VoiceJournal.jsx` and React UI.
*/

// Placeholder to avoid runtime errors if loaded accidentally.
function saveText() {
  console.warn('saveText called — deprecated. Use the React app journal instead.');
}

